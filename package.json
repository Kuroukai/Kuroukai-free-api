{
  "name": "kuroukai-free-api",
  "version": "2.0.0",
  "description": "API para gerar e gerenciar chaves de acesso gratuitas",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "node test-api.js",
    "build": "cd dashboard && npm install && npm run build && cd .. && mkdir -p public && cp -r dashboard/dist/* public/",
    "build:dashboard": "cd dashboard && npm run build",
    "install:all": "npm install && cd dashboard && npm install"
  },
  "keywords": [
    "api",
    "discord",
    "access-keys",
    "free"
  ],
  "author": "Kuroukai",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^8.0.1",
    "express-validator": "^7.2.1",
    "helmet": "^7.1.0",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
