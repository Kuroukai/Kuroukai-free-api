# Environment Variables para Railway
# Copie este arquivo para .env e configure as variáveis necessárias

# Porta do servidor (Railway define automaticamente)
PORT=3000

# Configurações de ambiente
NODE_ENV=production

# Database path (Railway gerencia automaticamente)
# DATABASE_PATH=./keys.db

# Configurações de CORS (opcional)
# CORS_ORIGIN=*

# Rate limiting (opcional)
# RATE_LIMIT_WINDOW=15
# RATE_LIMIT_MAX=100

# Configurações de chave padrão
# DEFAULT_KEY_HOURS=24
# MAX_KEY_HOURS=168

# CONFIGURAÇÕES OBRIGATÓRIAS PARA PRODUÇÃO
# Defina estas variáveis no Railway para segurança:

# Senha padrão do admin (OBRIGATÓRIO)
ADMIN_DEFAULT_PASSWORD=your_secure_admin_password

# Segredo da sessão (OBRIGATÓRIO - use um valor aleatório)
SESSION_SECRET=your_very_long_random_session_secret_at_least_32_chars

# Segredo JWT (OBRIGATÓRIO - use um valor aleatório)
JWT_SECRET=your_very_long_random_jwt_secret_at_least_32_chars